# Gerar lista de quadrados de 1 até n
def gerar_quadrados(n):
    # range(1, n+1) gera números de 1 até n (incluindo n)
    return [x ** 2 for x in range(1, n + 1)]

n = int(input("Digite um número inteiro positivo: "))
print(gerar_quadrados(n))